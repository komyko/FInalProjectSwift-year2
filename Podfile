# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'sqliteDB-SampleiOS' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'GRDB.swift'
  pod 'SideMenu'
  pod 'lottie-ios'

  # Pods for sqliteDB-SampleiOS

  target 'sqliteDB-SampleiOSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'sqliteDB-SampleiOSUITests' do
    # Pods for testing
  end

end
